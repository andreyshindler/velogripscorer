<?xml version="1.0" encoding="utf-8"?>
<!-- App themes (LIGHT base). values-night/themes.xml swaps to the Material dark
     base so framework widgets, dialogs and the status bar follow dark mode. -->
<resources>
    <style name="AppTheme.NoActionBar" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_bg</item>
        <item name="android:colorBackground">@color/app_bg</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_muted</item>
        <item name="android:colorAccent">@color/velogrip_green</item>
        <item name="android:statusBarColor">@color/nav_bar_bg</item>
    </style>

    <style name="AppTheme.DarkActionBar" parent="@android:style/Theme.Material.Light.DarkActionBar">
        <item name="android:windowBackground">@color/app_bg</item>
        <item name="android:colorBackground">@color/app_bg</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_muted</item>
        <item name="android:colorAccent">@color/velogrip_green</item>
        <item name="android:statusBarColor">@color/nav_bar_bg</item>
    </style>
</resources>
